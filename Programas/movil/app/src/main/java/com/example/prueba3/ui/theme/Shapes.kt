package com.example.prueba3.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val Shapes = Shapes(
    small = RoundedCornerShape(8.dp), // Para elementos pequeños como chips
    medium = RoundedCornerShape(16.dp), // Para elementos medianos como tarjetas
    large = RoundedCornerShape(24.dp) // Para contenedores más grandes
)
